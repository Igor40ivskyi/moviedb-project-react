import {MoviesList} from "../../components";
import Header from "../../components/Header/Header";

const MoviesPage = () => {
    return (
        <div>
            <Header/>
            <MoviesList/>
        </div>
    );
};

export {MoviesPage};